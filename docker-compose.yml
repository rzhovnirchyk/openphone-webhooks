version: "3"

services:
  node:
    build: ./
    volumes:
      - ./prisma/database.db:/srv/app/prisma/database.db:delegated
    ports:
      - "${VIRTUAL_PORT}:3000"
    networks:
      - nginx-proxy
    restart: unless-stopped

networks:
  nginx-proxy:
    external:
      name: nginx-proxy
